# Display options
display:
  name: NFive
  game: Custom
  map: Los Santos

# Set logging output level per component
# Levels: Trace|Debug|Info|Warn|Error
log:
  core: Debug
  rpc: Debug
  events: Debug

  plugins:
    # Default log level for plugins unless overriden
    default: Debug

    # Override indervidual plugin log level
    NFive/plugin-example: Error
